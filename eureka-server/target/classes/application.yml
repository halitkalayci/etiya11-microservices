# Yerel bootstrap ayarları. Asıl konfigürasyon Config Server'dan
# (configs/eureka-server/*) çekilir.
spring:
  application:
    name: eureka-server
  profiles:
    active: dev
  config:
    import: configserver:http://localhost:8888
