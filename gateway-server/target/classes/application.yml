# Yerel bootstrap ayarları. Asıl konfigürasyon Config Server'dan
# (configs/gateway-server/*) çekilir.
spring:
  application:
    name: gateway-server
  profiles:
    active: dev
  config:
    import: configserver:http://localhost:8888
